import { Box, Divider } from "@mui/material";
import CustomButton from "./CustomButton";

const Users = () => {
	return (
		<Box>
			<CustomButton variant="contained">Create User</CustomButton>
			<Divider />
		</Box>
	);
};

export default Users;
